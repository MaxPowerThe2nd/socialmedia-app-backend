services:
  mongodb:
    image: mongo:latest
    container_name: social-media-db
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=socialmedia
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: